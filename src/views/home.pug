extends base
include mixins/video

block content
    // Display videos in grid or show empty message
    div.video-grid
        each video in videos
            +video(video)
        else
            span.empty__message No videos found